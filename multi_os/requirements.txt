# Core (required)
psutil>=5.9.0
platformdirs>=4.0.0
httpx>=0.27.0
typer[all]>=0.12.0
rich>=13.0.0
pydantic>=2.0.0
ollama>=0.3.0

# Optional GPU (NVIDIA)
nvidia-ml-py>=12.0.0

# UI options
gradio>=4.0.0          # Web Control Center (browser)
textual>=0.50.0        # Pure terminal TUI (air-gapped)
